#include <stdio.h>

int main(void)
{
    int no;

    printf("整数を入力してください\n"); scanf("%d", &no);

    printf("2倍は%dです\n",2 * no ); scanf("%d", &no);
    printf("2乗は%dです\n",9 * no ); scanf("%d", &no);

    return 0;
}